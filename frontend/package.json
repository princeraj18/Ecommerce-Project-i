{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix latest run dev",
    "build": "npm --prefix latest run build",
    "lint": "npm --prefix latest run lint",
    "preview": "npm --prefix latest run preview"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0"
  }
}
